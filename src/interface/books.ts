export interface IBook {
    name: string,
    title: string,
    price: number,
    img: string,
    id: string,
    quantity: number,
  }

  export interface IBookClient {
    name: string,
    title: string,
    description: string,
    price: number,
    id: string,
    quantity: number,
    img: string

  }